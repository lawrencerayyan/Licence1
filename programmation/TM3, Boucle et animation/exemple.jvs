void main() {
    clear();
    int A = readInt("Entrer un nombre");
    int C = 0;
    int k = readInt("Entrer un nombre pour K");
    int B = 1;
    println("a0=" + A);
    while (B < k) {
        B++;
        if (A % 3 == 0) {
            C = A / 3;
            println("a" + k + "=" + A + "/3=" + C);
            A = C;
        } else {
            C = (2 * A) + 1;
            println("a" + k + "= 2*" + A + "+1 =" + C);
            A = C;
        }
    }
}
