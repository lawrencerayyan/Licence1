void main() {
    int x = readInteger("Quel est votre x ?");
    int y = readInteger("Quel est votre y ?");
    for (int i = 0; i < y; i++) {
        if (i != x) {
            print("-");
        } else {
            print("|");
        }
    }
}
